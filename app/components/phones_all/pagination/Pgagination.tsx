import {FC} from 'react'
import {useAppSelector} from "@/app/hooks/redux";

const Pagination: FC = () => {
    const {} = useAppSelector(state => state.phonesDataReducer)

    return <div>

        adasd
    </div>
}

export default Pagination